import { Typography } from '@mui/material';

const EmptyUser = () => (
  <Typography data-testid="no-user-available" variant="body1">
    Oops! No user(s) available
  </Typography>
);

export default EmptyUser;
